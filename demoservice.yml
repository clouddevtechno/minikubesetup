apiVersion: v1
kind: Service
metadata:
  name: service1
spec:
  selector:
    app: tomcatserver
  ports:
  - protocol: TCP
    port: 80
    targetPort: 8080
    nodePort: 30000
  type: NodePort

# nodePort -  30000-32767
  
 